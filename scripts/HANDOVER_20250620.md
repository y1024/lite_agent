# Handover — 2026-06-20 Session Context

## 当前状态总览

### ✅ 已完成 & 已部署 (VPS1 生产运行中)

**1. StraTA 战略焊接 + DAG 全局约束** (commits: `e05dba9`, `6280e73`, `ec1a20a`)
- `PLANNER_PROMPT` 输出 `global_strategy` + 感知 `{max_steps}` 预算
- Worker `_build_prompt` 焊接 `## 总体目标` + `## 全局战略`
- `SubtaskDAG` 支持 `global_strategy` + `max_depth` 落闸 + to_dict/from_dict 双向兼容
- Fail-Fast: `N×5 > budget×1.5` 弹性拦截（1.5x 浮动阈值，不否决紧凑规划）
- 运行期硬熔断: `total_steps >= effective_max_steps` → SKIP 剩余
- 用户提额: `[steps=N]` 后缀，agent.py `_run_orchestrated` 入口解析
- 配置: `dag_max_total_steps: 30`, `dag_max_total_tokens: 200000`

**2. 会话字节预算优化** (commit: `024f57f`, `f317c68`)
- `_get_message_size`: 计入 content/role/reasoning/tool_calls
- `_group_messages_to_blocks`: 原子块防孤儿 tool 消息
- `get_history`: 字节预算 + working 3x 宽限 + Phase 3 孤儿清洗
- 配置: `max_history_bytes: 40000`

**3. 非 Root 降权迁移**
- 用户: `liteagent`，组: `liteagent`
- 组权限: `systemd-journal`, `adm`, `docker`
- sudoers 白名单: `/etc/sudoers.d/liteagent` — 仅 `systemctl restart lite-agent` 和 `nginx`
- sentinel 降级运行 (Option A)，跳过 root 专属检查
- `mail-statement-parser` 迁移至 `/home/liteagent/`
- 硬编码路径 `sed` 替换: `ops_backup.py`, `ops_billing.py`
- systemd service 已切换 User/Group/WorkingDirectory
- `config.json` 中 `project_root` 已指向 `/home/liteagent/lite_agent`
- 回滚: `/etc/systemd/system/lite-agent.service.bak` 备份

**4. 慢响应修复**
- 根因: `max_steps_per_goal` 被误设为 30（影响所有消息的 Agent 主循环）
- 修复: 改回 10（DAG 预算 `dag_max_total_steps: 30` 独立，不受影响）
- 已重启服务，`liteagent` 用户运行正常

### 全部测试通过 (80 assertions, liteagent 用户)
```
test_straTA_budget.py      32 passed
test_session_budget.py     20 passed
test_session_upgrade.py    19 passed
test_dag_tool_alloc.py      9 passed
```

---

## 🔴 待执行：安全加固 (shell=True 消除)

### 已 Review 的方案文件
`c:\Users\chenl\.gemini\antigravity\brain\7db82100-dc5c-432a-a776-086b2b7b4bd6\implementation_plan.md`

### 方案覆盖的 7 个文件 (方案正确，待实施)
| 文件 | 改动 |
|---|---|
| `skills/ops_sys.py` | shell=True → list args + Python 解析 (/proc/loadavg, top, free, df, ps) |
| `skills/ops_security.py` | shell=True → list args + 直接读 /var/log/auth.log 用 Python grep |
| `skills/ops_workspace.py` | `ops_web_fetch` URL 校验 (http/https scheme regex) |
| `skills/ops_blog.py` | `export_dir` path traversal 防护 (abspath + prefix check) |
| `skills/ops_decision.py` | `task_type`/`trace_id`/`run_id` sanitize，防路径穿越到 data/committee 外 |
| `skills/ops_sentinel.py` | `/home` 遍历改用 `os.path.join` + `os.path.normpath` + prefix check |
| `skills/ops_rss.py` | `_v2ex_reply_count` URL + V2EX_TOKEN regex 校验 |

### 🔴 Review 发现方案遗漏的 3 个文件 (需追加)
| 文件 | 行号 | 风险 | 修复方向 |
|---|---|---|---|
| `skills/ops_logs.py` | L43, L91 | `shell=True` + LLM 生成的 `keyword`/`service_name` → 命令注入 | 改 list args，管道改 Python |
| `skills/ops_crontab.py` | L13, L52 | L52 `shell=True` + 用户 `script_path` → 代码执行 | `['bash', script_path]` list 形式 |
| `skills/ops_self_check.py` | L167, L178 | `shell=True` + LLM 生成的 `svc` → 命令注入 | `['journalctl', '-u', svc]` list 形式 |

### Review 发现的细节缺陷
- `ops_blog.py`: 除 `export_dir` 外，`title` 字段（来自博客 API）也可能含 `../`，需额外 sanitize

### 建议
实施时写 `scripts/test_security_hardening.py` 覆盖核心改动（shell=False 验证、路径穿越拦截、URL 校验）。

---

## 参考：关键文件位置

| 文件 | 用途 |
|---|---|
| `task_orchestrator.py` | DAG 编排核心 (Planner + 预算 + Fail-Fast) |
| `worker_agent.py` | 子任务执行 (StraTA prompt 焊接) |
| `subtask_dag.py` | DAG 数据结构 (global_strategy + 深度校验) |
| `agent.py` | Agent 主循环 + [steps=N] 解析 |
| `session.py` | 会话管理 (字节预算 + 原子块) |
| `config.json` / `config.example.json` | 所有新增配置项 |
| `skills/` | 技能库 (待安全加固) |
| `scripts/test_*.py` | 测试套件 |

## SSH 到 VPS1
Windows 原生 `ssh vps1` 可用（Git Bash 的 ssh 有 libcrypto 密钥解析问题）。
