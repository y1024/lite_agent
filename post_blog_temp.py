import sys
sys.path.insert(0, '/root/lite_agent')
from skills.ops_blog import blog_publish_article

title = "双 AI 协同高效开发实录：RssAdapter & lite_agent 架构演进与用户画像蒸馏落地"
content = """# 双 AI 协同高效开发实录：RssAdapter & lite_agent 架构演进与用户画像蒸馏落地

随着大语言模型（LLM）能力的演进，利用 AI 进行辅助编程已成为日常。然而，在面对包含多仓库、多技术栈（如 C# Backend 与 Python AI Agent 结合）、跨本地与 VPS 双端部署的复杂生态系统时，如何高效进行多模块协同、排查并发冲突并沉淀高质量代码，仍然是一个极具挑战的课题。

今天，我们记录了一场高效的双 AI（Claude Code 客户端与 Antigravity 服务端）协同开发实录。在短短一天内，我们向 3 个核心仓库提交并部署了 5 个 Pull Requests，涵盖从架构重构、服务守护、并发隐患修复、用户画像长期记忆沉淀到外部高考数据库接入的全流程。

---

## 🛠️ 项目生态背景

我们的个人助理与信息流获取生态主要由以下三个项目构成：
1. **RssAdapter (C# .NET 8)**：部署在 Mac 本地，提供基于 Playwright（有头模式）的高质量网页渲染与抓取接口（`/api/Url2Md`），绕过反爬机制将动态页面转换为干净的 Markdown。
2. **lite_agent (Python 3)**：部署在 VPS 上，是多通道（飞书、Telegram、钉钉、企业微信）的智能 Agent。负责核心 LLM 任务编排、工具调用、定时任务运行和长期记忆引擎。
3. **rssnextui (Vue / Vite)**：前端阅读器，实现文章的阅读、AI 点评与本地秒级解析查看。

---

## 🚀 今日核心战果与架构演进

### 1. RssAdapter 插件式解析架构与秒开体验 (PR #2)
在以往的设计中，RssAdapter 对不同站点的解析逻辑混杂在一起，维护成本极高。
- **重构设计**：引入了 12 个重点站点（如微信公众号、知乎、V2EX、Linux.do 及 9 个通用新闻站）的结构化解析器插件。
- **秒开优化**：配合 `rssnextui` 新增“查看原文”功能，前端发起请求后由 Mac 端 RssAdapter 动态提取并利用 marked 和 DOMPurify 渲染，提供无污染、高安全的秒开解析。

### 2. LaunchAgent 服务守护与 KeepAlive 稳固
为了确保 Tailscale 局域网内 VPS 能稳定连接 Mac 端 RssAdapter，我们淘汰了不稳定的前台挂起或临时脚本，编写并注册了 Mac plist 描述符，利用 `LaunchAgent` + `KeepAlive` 机制对 RssAdapter 进行进程级守护与开机自启。

### 3. lite_agent 路由纠偏与 HedgeDoc 幻觉消除 (PR #3 & #4)
针对实际运行中 LLM 的不合理路由与过度推理行为进行了拦截：
- **路由纠偏**：修改 `ops_web_fetch` 与 `web_clip` 两个技能的 description，在描述中显式指定“微信/知乎等动态内容首选 web_clip”，阻止 LLM 在遇到公众号链接时选择 curl 导致失败并进入 `pip install` 的死循环。
- **消除 HedgeDoc 虚构幻觉**：对于未达 2500 字或无图的短文，web_clip 不再上传至 HedgeDoc 而是直接返回 Markdown，并在返回的元数据行中显式加上状态说明（`未达上传阈值，仅返回纯文本`），完美截断了 LLM 凭空捏造“已成功上传 HedgeDoc”的幻觉。

### 4. 个人画像（persona.md）蒸馏体系落地 (PR #5 & #6)
为了能将个人工作和偏好完全迁移至 Agent 会话，并让未来的任何智能应用都能快速了解用户，我们设计并上线了 **Persona 画像蒸馏体系**：
- **流动通路**：IM 聊天记录 → 日常自动增量提取 → `data/persona.md` 主档案 → 自动注入 System Prompt 提示词。
- **七维画像**：划分了“身份与角色”、“工作偏好”、“技术栈熟练度”、“当前进行中项目”、“已知决策”、“个人事实”和“⏳ 待确认”七个维度。
- **合并策略**：采用原子化临时写入和重命名，将画像切分为 `### 手动校正`（用户编辑的偏好，永不磨损）与 `### LLM 自动提取`（每次蒸馏整体替换，高置信度刷新）。
- **双重安全网**：在 LLM 画像提取阶段与文件写入阶段，部署了双重正则表达式黑名单，全行级别过滤 API Keys、私钥及密码凭证，宁可漏报也绝不误纳。
- **IM 控制能力**：新增了 `/persona` 命令展示画像大纲，并支持通过 `/persona confirm <序号> [分类]` 瞬间将 `⏳ 待确认` 条目升格到指定的“手动校正”子类中。

### 5. 高考志愿辅助数据库 7 技能接入 (PR #7)
将超过 300 万行的 SQLite 关系型高考志愿数据库挂载至 Agent 体系：
- **七大技能**：提供分数查位次、院校历年录取线、专业细分数据、冲稳保梯度志愿推荐等技能。
- **防注入安全策略**：使用只读连接模式（`mode=ro`），结合严格的 SELECT 检测和危险 DDL 关键字过滤，杜绝外部输入对本地库的破坏。

---

## 📌 技术债管理与持续演进 (Issue #8 ~ #11)

对于代码中存在的次要隐患，我们当即开设了 4 个跟进 issue，保持仓库整洁与后续敏捷迭代：
- **#8**：解决 SQL 黑名单在模糊匹配学校名称时可能产生的误伤问题。
- **#9**：对 LIKE 通配符进行转义处理，收紧查询安全边界。
- **#10**：将硬编码的数据库路径配置化，提高应用迁移性。
- **#11**：对异常信息进行友好过滤，避免直接将 `FileNotFoundError` 抛给 LLM 造成系统误判。

---

## 🧠 双 AI 协同开发模式的启示

在本次开发中，**Claude Code** 在客户端（处理 Python Agent 记忆逻辑及业务技能）进行前沿编写，**Antigravity** 在服务端（处理 C# 后端服务和架构维护）提供合并、核验与环境构建。

双 AI 协同在遇到多端联调时表现出了惊人的开发速度：
1. **秒级定位问题**：通过系统日志实时捕获 LLM 路由的系统反应，精准修补描述。
2. **闭环自动验证**：PR 合并后直接通过 VPS 远程命令行触发手动一分一段数据蒸馏、短 URL 强制上传等用例，闭环验证服务健康状况。
3. **流程规范化**：严格遵循“本地主分支 merge -> 推送 GitHub -> VPS 部署拉取 -> 重启服务 -> 状态回检”的生产部署流水线，不绕过 Git 协作规则，极大地保护了项目结构的干净与安全。

未来的智能助理不仅会拥有更好的检索增强（RAG），更会通过这种流动的 **Persona 提炼架构** 建立更加温暖、深刻的专属个人链接。

---
*记录时间：2026年6月14日* """

res = blog_publish_article(title, content)
print("PUBLISH_RESULT:", res)
